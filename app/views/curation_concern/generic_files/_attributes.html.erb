<table class="table table-striped <%= dom_class(curation_concern) %> attributes">
  <caption class="table-heading"><h2>Description</h2></caption>
  <tbody>
  <%= curation_concern_attribute_to_html(curation_concern, :creator, 'Author(s)') %>
  <%= curation_concern_attribute_to_html(curation_concern, :description, 'Abstract') %>
  <%# curation_concern_attribute_to_html(curation_concern, :created, 'Date Completed') %>
  <tr>
    <th>Access Rights</th>
    <td>
      <ul class="tabular">
        <li class="attribute permission"><%= link_to_edit_permissions(curation_concern) %></li>
      </ul>
    </td>
  </tr>
  <%= curation_concern_attribute_to_html(curation_concern, :embargo_release_date, "Embargo Release Date") %>
  <%= curation_concern_attribute_to_html(curation_concern, :identifier, "Digital Object Identifier (DOI)") %>
  <%= curation_concern_attribute_to_html(curation_concern, :contributor, "Contributing Author(s)") %>
  <%= curation_concern_attribute_to_html(curation_concern, :subject, "Keyword(s) or phrase(s)") %>
  <%= curation_concern_attribute_to_html(curation_concern, :language, "Language") %>
  <%= curation_concern_attribute_to_html(curation_concern, :publisher, "Publisher") %>
  <%= render "characterize_details", curation_concern: curation_concern %>
  </tbody>
</table>
